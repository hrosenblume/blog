/**
 * Shared Tailwind class strings for consistent styling across the app.
 * Use these instead of duplicating class strings in components.
 */

// Table styles (admin pages)
export const tableHeaderClass = 'px-6 py-3 text-left text-xs font-medium text-muted-foreground uppercase tracking-wider'
export const tableCellClass = 'px-6 py-4 whitespace-nowrap text-sm'
export const tableRowHoverClass = 'hover:bg-accent/50'

// Card styles
export const cardClass = 'bg-card rounded-lg shadow'
export const emptyStateClass = 'bg-card rounded-lg shadow p-8 text-center text-muted-foreground'

// Mobile card styles (admin pages)
export const mobileCardClass = 'bg-card rounded-lg shadow p-4'

